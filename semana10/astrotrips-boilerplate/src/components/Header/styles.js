import styled from 'styled-components';

export const HeaderWrapper = styled.header`
    padding: 18px 0 ;
    position: absolute;

    > img {
        width: 150px;
        padding-left: 18px;
    }
`